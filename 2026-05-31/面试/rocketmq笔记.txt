1、消息堆积中的消息并不是永久存储在消息服务器上，会有过期机制，默认是3天
Q:这三天的过期消息能通过回溯消息进行回溯嘛？


2、定时消息
消息服务端能够存储延迟的消息类型，但是只能存储特定延迟级别的消息。
Q:我猜想定时消息的队列肯定和我们的普通队列不一样，是另一个特殊的队列。可能就是安装我们呢的延迟级别产生对应的特殊队列？



Q:延迟级别有那几种呢？

3、消息重试机制
Q:如果消息在消费时发生异常，消息中间件就会重新投递消息。这个判断是不是使用ACK确认机制没有返回成功呢？


消息消费者还需要通过NameServer去找到消息生产者，
Q:我有时会认为消息消费者只是对接注册中心的，原来还需要和生产者对接，那么这块的调用或者关系是怎样的呢？


Q:文章中说的路由信息是什么？是消息服务器的注册地址嘛？


Q:broker消息服务器下线后，注册中心需要隔10s感知到，但它不会立马通知生产者？还反过来需要生产者提供容错机制，这是为何？
A:是为了简化注册中心的复杂度，生产者那边所提供的容错机制是是1、故障规避机制，当我们第一次发生消息到Broker失败的时候，接下来的5分钟之内发送就不会选择这个故障的Broker了
这也是一个消息发送的高可用设计



Q:BrokerName 是每个Broker都不一样，还是一个主从架构公用一个BrokerName呢？

Q:为什么一个topic上会创建4个读和写队列呢？

Q:路由发现是根据主题来查找的，那么生产者刚启动的时候，注册中心中的topicQueueTable数据应该是空的

Q:似乎这个topic是由Broker进行创建的，而不是在生产者创建的？


整个的大前提是开启了自动创建topcic的机制

为什么？为什么？整个过程中生产者或Broker在找不到对应的topic路由信息的时候，都会使用默认的topic路由信息，并且基于此在Broker中创建对应的新topic的路由信息

what?? 不会吧，首次使用我们定义的topic名称时，肯定是没有这个新topic的路由信息的。那么，我该如何去让Broker创建topic信息呢？其中的办法之一就是使用默认的topic路由信息
来发送消息到Broker，Broker中会校验一次这个topic的路由信息，如果不存在会使用默认的topic路由信息去创建对应的新topic路由信息

Q:一直所说的路由信息都是有哪些?
就是我们说的路由元信息，而且这个路由元信息是持久化在Broker中的，NameServer中的路由信息是来自Broker中的心跳包的并且存储在内存中


重试的时间间隔是多少呢？








为什么在生产环境中不建议使用自动创建topic呢？

生产者中如果在选择消息队列的时候发现这个Broker不可用，那么默认接下来这个Broker是不会再使用的

但如果开启故障延迟机制，那么会在5分钟之后再次去将有问题的Broker加入到我们呢的消息队列的选择中


如果发送失败，那么重试的







