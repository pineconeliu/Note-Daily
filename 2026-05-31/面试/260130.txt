抽奖的前置规则在抽奖中是一个什么行为。其实它可以被抽象为一种策略行为，比如；黑名单抽奖策略、权重抽奖策略、白名单抽奖策略等。而这些策略规则是一种互斥行为，比如走了黑名单规则，就不应该在继续走权重规则了。那么对于这样的情况，责任链的设计就更加合适了。

责任链的链路执行有它本身的优势，自身的实现就可以从一个链转入到下一个。那么对于普通策略规则的过滤一种是for循环顺序执行，另外一种借助组合模式的思想，创建出二叉树结构的调用链路关系。本节就是这种方式实现规则树模型。

使用Redis的延迟队列加阻塞队列这种双重队列来

这里是延迟的方法消息到 Redis 队列中。以此来减缓消费。（这里是一个双重减缓，一个是延迟队列，一个是定时的任务调度）
Q：这段代码是为了请求扣减库存的时候会重复执行
A：可以重复执行，即使队列中有相同的数据，也不怕，只要定时任务执行中每次获取到就行。作为消费端是

第一重：使用分布式锁防止库存超卖
第二重：假设Redis扣减没有控制好，后续的延迟消费扣减库存也能有效的控制并发，where条件中也是加上库存大于0的操作

延迟队列 + 任务趋势更新活动sku库存

缓存的处理不能加入到事务之中



防重的幂等使用场景---支付的时候触发一次新增抽奖次数



1、MySQL的MVCC只有在快照读的时候才能避免幻读


通过对每个库存的数量创建库存锁的key，然后对这个key尝试加锁

这段代码是一个基于Redis的库存扣减和防超卖机制实现。让我详细解释每个部分：
为什么需要这个锁？
防超卖兜底：

即使后续有"恢复库存"、"手动增加库存"等操作

因为每个库存值对应的key都被锁住了

其他请求无法使用这个库存

LLM 加锁时间（防止死锁）


Redis库存扣减
库存超卖有以下3个可能：
恢复库存、手动增加库存的时候都会出现超卖
1、Redis宕机，我
加锁的好处还能有效应对运营人员手动增加库存


可以这样说：虽然我们的Redis是原子性操作，但我们的代码不是，所以在高并发下还是会出现超卖的情况，
所以我们需要加上Redis的分布式锁，主要操作是：通过对每个库存的数量创建库存锁的key，然后对这个key尝试加锁，
加锁了之后，如果再次出现这个相同的同样的key就会加锁失败，返回false。


Redis的事务处理？




   /** 扣减库存，站在后端的角度上需要从2个方面考虑：1、一个是不能超卖，扣减库存正常 2、要效率高才能，尤其是在这种高并发的情况下（？就是你认为抽奖是一个高并发的业务场景吗？）
     * 3、在高并发场景下，既要够快也要抗压。
     * 我们知道数据库的资源是很宝贵的，为了很好的保护好数据库资源，我们会使用Redis和MQ中间件，Redis本身是一个基于内存操作的单线程中间件，执行起来会很快。
     * 主要实现步骤是：先提前将我们数据库中奖品的库存信息保存到我们的Redis中，数据请求直接和Redis接触，同时使用Redis的原子性操作decr方法进行扣减库存。
     * 因为会存在库存的恢复场景，比如运营一次性新增多个库存，那可能就会存在超卖的情况，我们就需要加锁进行兜底，对库存加锁之后，如果遇到需要用到这个i库存的情况
     * 就会发现已经加锁了，直接返回false表示失败。这部分的操作也只是预扣减库存，真正扣减库存是在数据库中的，但Redis本身执行也很快，请求完Redis之后也不能
     * 直接把请求给入到数据库中。我们可以使用MQ来解决，MQ本身是一个消息触发的机制，它本身能做到削峰填谷。我们可以使用延时队列接收库存后续的操作，让MQ慢慢消费
     * 消息，数据库也能得到很好的保护
     */
   

Bean的创建过程中，实例化阶段 -> 属性填充 -> 初始化阶段
   
为什么构造器注入的循环依赖无法通过三级缓存实现呢？
1、


   
解决流程（以A、B循环依赖为例）
创建A

实例化A（调用构造方法）

将A的ObjectFactory放入三级缓存

填充A的属性，发现依赖B

创建B

实例化B

将B的ObjectFactory放入三级缓存

填充B的属性，发现依赖A

获取A

从三级缓存获取A的ObjectFactory

执行ObjectFactory.getObject()获取A的早期引用（可能是代理对象）

将A的早期引用放入二级缓存，从三级缓存移除

B完成属性注入

B创建完成后，继续填充A的属性

A完成初始化，放入一级缓存

从二级缓存移除A


Q1：Spring一定能解决循环依赖吗？
不一定，以下情况无法解决：

构造器注入的循环依赖，A的实例化过程中需要B，而B的实例化过程中需要A。造成死锁

原型作用域的循环依赖，每次创建都是新的实例，所以三级缓存会不起作用

@Async注解的Bean（因为需要AOP代理）







模板方法一般和什么其他设计模式一起使用呢？

规则树的使用

还有我们的抽奖流程，前中后的处理






